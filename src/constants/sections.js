// Top-level navigation sections. The Sidebar renders one entry per id in
// the same order; the global store validates ``activeSection`` against
// this list when restoring from persistence.
export const SECTION_IDS = ["home", "risk", "macro", "adaptation", "workspace", "settings"];
